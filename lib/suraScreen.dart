import 'package:flutter/material.dart';

class SureScreen extends StatelessWidget {
  static const String RouteName = "SuraScreenRoute";
  const SureScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Sura"),
      ),
    );
  }
}
